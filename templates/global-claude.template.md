# Helmsman Global Memory

> **Scope**: All projects across all repos in this workspace
> **Updated by**: User (manually) or Orchestrator (after memory-distillation prompt, user-approved)
> **Location**: `memory/CLAUDE.md`
>
> This file is loaded at the start of every Helmsman session.
> Keep it concise — the Orchestrator reads this every time.

---

## Workflow Preferences

> How this developer likes to work with Helmsman.

- **PRD style**: {{e.g., "User prefers concise PRDs with explicit non-goals; no lengthy preamble"}}
- **Design preference**: {{e.g., "User wants ADRs for every significant decision, even small ones"}}
- **Task granularity**: {{e.g., "Prefer 2-4h tasks over larger chunks"}}
- **Review strictness**: {{e.g., "Strict on security issues; lenient on nits unless they're in public APIs"}}
- **Approval style**: {{e.g., "User approves per task, not per stage-batch"}}

---

## Communication Style

- {{e.g., "Be direct. Skip preamble. List findings, then recommendation."}}
- {{e.g., "Always show the diff when proposing changes to an artifact."}}

---

## Cross-Project Learnings

> Patterns and insights observed across multiple projects.

- {{e.g., "Auth flows in this org always require explicit audit logging — check this at design stage"}}
- {{e.g., "DB migrations take longer than estimated — add 50% buffer to task effort"}}

---

## Repos in This Workspace

> Quick reference — full details in `manifest.yaml`.

| Repo | Language | Primary use |
|---|---|---|
| {{repo_name}} | {{language}} | {{description}} |

---

## Notes

{{freeform_notes}}
